{
	"name": "url-to-md",
	"version": "0.8.0",
	"private": true,
	"scripts": {
		"publish": "pnpm run cf-typegen && wrangler deploy",
		"start": "pnpm run cf-typegen && wrangler dev --remote",
		"cf-typegen": "wrangler types",
		"type-check": "pnpm run cf-typegen && tsc"
	},
	"devDependencies": {
		"typescript": "^5.8.3",
		"wrangler": "^4.10.0"
	},
	"dependencies": {
		"@cloudflare/puppeteer": "^1.0.1",
		"@types/node": "^22.14.1",
		"react-tweet": "^3.2.2"
	}
}
